#include <stdio.h>

/**
 * main - printing 16 in base 16
 * Return: 0 (successful)
 */
int main(void)
{
	int x;

	char y;

	for (x = 0; x < 10; x++)
		putchar(x + '0');
	for (y = 'f'; y < 'f'; y++)
		putchar(x);
	putchar('\n');
	return (0);

}
