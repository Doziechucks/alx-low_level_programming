#include <stdio.h>
/**
 * main - printing number combos
 * Return: 0(successful)
 */
int main(void)
{
	int x;

	for (a = 0; x < 10; x++)
	{
		putchar(x + '0');
		if (x < 9)
		{
			putchar(',');
			putchar(' ';)
		}
	putchar('\n');
		return (0)
	}
}
