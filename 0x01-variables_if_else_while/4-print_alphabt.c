#include <stdio.h>
/**
 * main - print lowercase alphabet except q and e
 * Return: 0 if successful
 */
int main(void)
{
	char x;

	x = "a";
	while (x <= 'z')
		for (x != q; x != e; x++)
			putchar(x);
	putchar('\n')
	return (0);
}
