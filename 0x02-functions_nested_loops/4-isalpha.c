#include "main.h"

/**
 * _isalpha - check for alphabetic character
 * @c: upper case to be checked variable
 * Return: reurns 1 for alphabetic character and 0 for everything else
 */
int _isalpha(int c)
{
	if (c >= 65 && c <= 122)
	{	return (1);
	}
return (0);

}
